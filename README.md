# apple-music-unified
Self-contained ARM64 Docker image for downloading Apple Music in ALAC/Hi-Res/Atmos via web UI. Native ARM64 (no QEMU), based on apmyx + WorldObservationLog/wrapper. Single container, single compose, saves organized files to any mounted path.
